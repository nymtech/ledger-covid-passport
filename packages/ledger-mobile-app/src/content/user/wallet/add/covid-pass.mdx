Users can add the COVID certificate or pass issued by their health authority by scanning the QR code.

It will be decoded and added to their wallet, where it can be used to generate a private certificate that discloses
less information than the full certificate.
